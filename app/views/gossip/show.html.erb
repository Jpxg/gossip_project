<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div class="container">
      <h1 class='text-center  pb-4 mb-4'>Le gossip: </h1>
      <div class="card  mt-4 mb-4 bg-light border-success">
        <div class="row no-gutters">
          <div class="col-3 col-sm-2 m-auto text-center">
            <i class="fas fa-comments fa-4x text-success"></i>
          </div>
          <div class="col-9 col-sm-10">
            <div class="card-body">
              <h5 class="card-title"><%= @gossip.user.first_name %> <%= @gossip.user.last_name %></h5>
              <p class="card-text"><%= @gossip.title %></p>
              <p class="card-text"><%= @gossip.content %></p>
            </div>
          </div>
        </div>
      </div>
      <div class='row justify-content-around'>
      <div class='col-4 btn btn-success'><%= link_to "Aller sur la page de l'auteur", user_id_path(@gossip.user.id) %></div>
      <div class='col-4 btn btn-success'><%= link_to "Retourner à la liste des gossips", "/welcome/#{session[:username]}" %></div>
    </div>
  </body>
</html>
